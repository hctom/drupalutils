{% extends "@drupalutils/php.twig" %}

{# File comment #}

{% block fileComment %}
Database connections for the '{{ drupalutils.environment }}' environment.
{% endblock %}

{# PHP code #}

{% block phpCode %}
{# Databases #}
{% for databaseKey, targets in databases %}
{% for targetKey, databaseConnection in targets %}
{% if not loop.first %}

{% endif %}
$databases['{{ databaseKey }}']['{{ targetKey }}'] = {{ databaseConnection|var_export }};
{% endfor %}
{% endfor %}
{% endblock %}
